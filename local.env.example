# Copy: cp local.env.example local.env
# Used by scripts/local-deploy.sh — still loads the Discord token from AWS Secrets Manager.
# Point the SDK at your account (profile, region, secret name). Credentials: ~/.aws/credentials or env vars.

# AWS_PROFILE=default
# AWS_REGION=us-east-1
# AWS_SECRET_NAME=220_Discord_Token
